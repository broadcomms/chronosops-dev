apiVersion: cloud.google.com/v1
kind: BackendConfig
metadata:
  name: chronosops-backend-config
  namespace: chronosops
spec:
  # Long timeout for WebSocket and MJPEG streaming connections (24 hours)
  timeoutSec: 86400
  # Connection draining timeout
  connectionDraining:
    drainingTimeoutSec: 60
