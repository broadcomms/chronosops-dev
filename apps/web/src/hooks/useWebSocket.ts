/**
 * WebSocket hook - Re-exports from WebSocketContext for backwards compatibility
 *
 * @deprecated Import directly from '../context/WebSocketContext' instead
 */
export {
  useWebSocket,
  useWebSocketContext,
  useWebSocketChannel,
} from '../context/WebSocketContext';
